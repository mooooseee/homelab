services:
  nginx:
    image: nginx:latest
    container_name: nginx-web
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - homelab-network

networks:
  homelab-network:
    external: true
